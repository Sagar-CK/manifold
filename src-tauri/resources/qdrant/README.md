This directory stores platform-specific Qdrant binaries used by Manifold.

Do not add binaries manually. Use:

`pnpm setup:binaries`

Expected output binary names:
- macOS/Linux: `qdrant`
- Windows: `qdrant.exe`
